using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/**
 * The director class is a do not destroy object
 * stores all the information regarding game state
 * and loads the scenes with their appopriate values
 */
public class Director : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
